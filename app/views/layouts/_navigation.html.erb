
<nav class="navbar navbar-expand-lg fixed-top navbar-light bg-light">
  <a class="navbar-brand" href="<%= users_path %>"><i class="fa fa-paw"></i> Fluffy Tail</a>
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>

  <div class="collapse navbar-collapse" id="navbarSupportedContent">
    <ul class="navbar-nav mr-auto">
        <li class="nav-item">
        <li class="nav-item dropdown">
        <% if @current_user %>
        <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
          <%= @current_user.name %>'s Details
        </a>
        <div class="dropdown-menu" aria-labelledby="navbarDropdown">
          <a class="dropdown-item" href="<%= user_path(@current_user) %>">Details</a>
          <a class="dropdown-item" href="<%= edit_user_path(@current_user) %>">Edit</a>
          <div class="dropdown-divider"></div>
          <a class="dropdown-item" <%= link_to "Logout", logout_path, method: :delete %></a>
          
        </div>
      </li>

        <% else %>
        <li class="nav-item">
          <a class="nav-link" href="<%= new_user_path %>">Sign Up</a>
        </li>
        <a class="nav-link" href="<%= new_session_path %>">Login</a>
        <% end %>
        </li>


        <li class="nav-item">
          <a class="nav-link" href="<%= pets_path %>">Pets</a>
        </li>

        <li class="nav-item">
          <a class="nav-link" href="<%= locations_path %>">Locations</a>
        </li>

    </ul>


    <%= form_for "", url: pets_path, role: "search", method: :get do %>
      <%= text_field_tag :search, @search_term, placeholder: "Search Pets" %>
      <%= submit_tag ("Search") %>
    <% end %>

  </div>
</nav>


