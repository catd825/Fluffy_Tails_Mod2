<%= form_for @favorite do |f| %>
  <%= f.label :pet_id %>
  <%= f.collection_select :pet_id, @pets, :id, :name %>

    <%= f.submit %>
<% end %>
    
    <%= link_to "Back to all pets", pets_path %><br>